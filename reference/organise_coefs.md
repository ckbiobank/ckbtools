# organise coefs data frame either keep only exposure terms, or move them to the top

organise coefs data frame either keep only exposure terms, or move them
to the top

## Usage

``` r
organise_coefs(return, coefs, exposure_terms)
```
