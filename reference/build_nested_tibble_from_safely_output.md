# Create a tibble from the output of linear_model_safely(), logistic_model_safely(), cox_model_safely()

Create a tibble from the output of linear_model_safely(),
logistic_model_safely(), cox_model_safely()

## Usage

``` r
build_nested_tibble_from_safely_output(z)
```
